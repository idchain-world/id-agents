node_modules/
dist/
*.log
*.tgz
.DS_Store
nohup.out
temp/
workspaces/**
workspace/**

# Agent runtime checklist (loaded by the CLI agent, not source-controlled)
HEARTBEAT.md

# Personal configs — only default.yaml is tracked; anything else a user
# drops into configs/ stays local.
configs/*.yaml
!configs/default.yaml

# Environment and secrets
.env
.env.*
!.env.example
**/agent-wallets.json
**/.env.wallet
*.pem
*.key

# IDE and editor
.idea/
.vscode/
*.swp
*.swo

# Claude Code personal settings (use plugin instead)
.claude/

# Coverage and test artifacts
coverage/
.nyc_output/

# Security files (local only)
SECURITY.md
REVIEW_LOG.md
.xmtp/

# Session-level scratch (agent-created, not source)
AGENTS.md
.agents/
output/

# Local-only worktree artifacts (not part of the release patch)
.cursor/
/bin/
id-agents-demo-1.mp4
.testdb/
